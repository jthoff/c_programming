// Write a program that converts 27 degrees Fahrenheit to Celsius
// Use this formula C = (F - 32) / 1.8

#include <stdio.h>

int main (void)
{
	int celsius;

	celsius = (27 -32) / 1.8;
	printf ("%i degress Fahrenheit is \n", celsius);

	return 0;
}
